#!/usr/bin/env python3
"""Superseded.

The icons are now the ST monogram, drawn with the app's own typeface. No image
library is installed here and none of them render text, so the monogram is
rendered on a browser canvas and posted back by tools/icon-receiver.py.

To regenerate: start the receiver, open the app, and run the render snippet
against it. The design lives in that snippet, not here.
"""
